<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.s1651374.papadocapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DoctorAppListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReceptionistMenuActivity" />
        <activity android:name=".PatientInfoActivity" />
        <activity
            android:name=".ScheduleActivity"
            android:label="Schedule Appointment" />
        <activity android:name=".DoctorMenuActivity" />
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".PatientAnswersActivity" />
        <activity android:name=".SignInActivity"></activity>
        <activity android:name=".ReceptionistAppListActivity"></activity>
    </application>

</manifest>